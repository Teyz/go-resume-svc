version: "3"

services:
  go-resume-svc-app:
      command: reflex -sr '\.go$|\.mod$' -- sh -c "go run cmd/main.go"
      build:
        dockerfile: Dockerfile.local
        context: .
      depends_on:
        - go-resume-svc-db
        - go-resume-svc-redis
      environment:
        - DB_USER=root
        - DB_PASSWORD=root
        - DB_NAME=go-resume-svc-db
        - DB_HOST=go-resume-svc-db
        - DB_PORT=5432
        - HTTP_SERVER_PORT=3003
        - CACHE_HOST=go-resume-svc-redis
        - CACHE_PORT=6379
        - SERVICE_NAME=go-resume-svc
        - ENVRIONMENT=local
      volumes:
        - .:/go/src/app
      working_dir: /go/src/app
      ports:
        - "3003:3003"
  go-resume-svc-redis:
    image: redis:latest
    ports:
      - "6379:6379"
  go-resume-svc-db:
    image: postgres:latest
    environment:
      POSTGRES_USER: 'root'
      POSTGRES_PASSWORD: 'root'
      POSTGRES_DB: 'go-resume-svc-db'
    ports:
      - "5432:5432"